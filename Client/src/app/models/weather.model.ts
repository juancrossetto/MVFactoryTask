export class Weather {

        // Identificador del Tiempo
        WeatherID:number;

        // Ciudad
        Country:string;

        // Tempartura
        Temperature:number;

        // Humedad
        Humidity:string;

        // Nubes
        Clouds:string;

        // Presión
        Pressure:string;

        // Dirección del Viento
        WindDirection:string;

        /// Dirección del Viento
        WindSpeed:number;
}
